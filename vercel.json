{
    "version": 2,
    "builds": [
      {
        "src": "stenograph/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "200mb", "runtime": "python3" }
      },
      {
        "src": "build_files.sh",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "staticfiles_build"
        }
      }
    ],
    "routes": [
      {
        "src": "/static/(.*)",
        "dest": "/static/$1"
      },
      {
        "src": "/(.*)",
        "dest": "stenograph/wsgi.py"
      }
    ]
  }